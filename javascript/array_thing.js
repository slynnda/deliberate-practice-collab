let numberArray = [2, 1, 10];

function sumOfDifferences(arr) {
    arr = arr.sort(function(a,b){return b-a});
    for(let i=0; i<arr.length; i++){
        
    }
    index = arr.indexOf(arr[0]);
    if(let arrayIndex1 = arr[0], let arrayIndex2 = arr[1]; arrayIndex1 != undefined; arrayIndex1()){
  }
}

sumOfDifferences(numberArray);
